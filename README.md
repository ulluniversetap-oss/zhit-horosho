# ЖК «Жить хорошо» — лендинг

Статический прототип: один `index.html` + ассеты рядом. Без сборки, без зависимостей — открывается напрямую или локальным сервером.

## Структура

```
zhit-horosho/
├── index.html
├── assets/
│   ├── images/     фотографии, иконки офферов, мокап презентации
│   ├── icons/      pdf-badge.svg (кольцо «Обновлён 14.09.26»)
│   └── fonts/      RoadRadio-Bold.ttf (самохостится; Onest — из Google Fonts)
└── README.md
```

## Запуск

Любой статический сервер подойдёт, например:

```bash
npx serve .
# или
python3 -m http.server 8080
```

Открывать `index.html` напрямую двойным кликом тоже можно — все пути относительные.

## ⚠️ Готово

- `assets/images/hero-bg.jpg` — постер для hero-видео, кадр из `assets/videos/hero-bg.mp4`
  (вытащен через `ffmpeg -ss 0.5 -frames:v 1 -vf scale=1920:-1`).
- `assets/images/family-house.jpg` — фото дома уже на месте.
- В ипотечном калькуляторе вместо цветных кружков-монограмм — настоящие круглые
  иконки банков (`assets/icons/banks/`), только знак, без текста бренда: Сбербанк,
  Т-Банк, Альфа-Банк, ВТБ — официальные favicon-иконки банков (`.ico`, скачаны с
  `<банк>.ru/favicon.ico`, у Wikimedia Commons нашлись только логотипы-wordmark'и
  с текстом, не подошли под требование «только знак»); ЦентрИнвест — `.svg` с
  `centrinvest.ru/favicon.svg`. `.mcalc-mono` — круглый чип (`border-radius: 50%`,
  `overflow: hidden`), `<img>` внутри с `object-fit: contain`. Поле `logo` в
  `FALLBACK_BANKS` (index.html) и `assets/data/banks.json` — имя файла.

## Незаполненные данные

- Блок «Всё нужное — в пешей доступности»: тайминги рассчитаны автоматически по пешеходным
  маршрутам OpenStreetMap (Nominatim + Overpass + OSRM `foot`-профиль, расстояние; время —
  из расстояния при 5 км/ч, т.к. `duration` публичного OSRM-демо для пешего профиля выглядит
  некорректно быстрым и ему нельзя доверять напрямую) от условной точки дома — точный адрес
  Новосибирская, 37 пока не присвоен официально, поэтому взят геоцентр известного участка улицы
  (48.67926, 44.45265). **Требует финальной сверки, когда адрес/координаты дома будут официальными.**
  Текстовая приписка об этом под заголовком блока (`.walk-note`) убрана со страницы по просьбе —
  для посетителей это лишняя техническая деталь; сама методология и её ограничения остаются
  актуальными и описаны здесь для разработчика.
  - «Детский сад»: в OSM в разумном радиусе от точки дома не нашлось правдоподобного ближайшего
    детсада (ближайшие найденные — 4–5 км, явно не «ближайшие»). Оставлено «—» до уточнения вручную.
- Координаты ЖК на карте (`.walk-map-frame` iframe) обновлены на тот же условный геоцентр
  (48.67926, 44.45265) — тоже требует подтверждения после присвоения официального адреса.
- Блок «Горячая вода — всегда» скрыт (`display: none` на `.heat`) — сделано намеренно по просьбе,
  разметка и стили в файле сохранены (не баг, оставлять как есть).
- Блок «Готовая отделка» (`.finish`) — по просьбе снята белая подложка (`.finish-panel`
  раньше был `background: #ffffff; border-radius: 1.2rem;`, как у офферов/калькулятора).
  Вместо 3D-тура (`https://a-butik.ru/3d-tour/`) теперь видео с Kinescope
  (`https://kinescope.io/embed/9sNYxpy1rLUcgyCgq1h2Ez`) в том же `.iframe-full`
  контейнере — плеер сам по себе без автоплея (нет `?autoplay=1` в src и нет
  `autoplay` в атрибуте `allow`), запускается только по клику пользователя.
  Контейнер (`.finish-tour`) сначала был сделан во всю ширину секции (пробивал боковой
  паддинг `.finish-inner` отрицательными полями, чтобы совпадать по ширине с фото
  `.expert-lead__bg` выше) и уменьшен по высоте примерно на 30% (`padding-bottom` у
  `.iframe-full`). После снятия подложки собственный внутренний паддинг `.finish-panel`
  (`var(--space-80) 0`) задваивал внешние отступы секции — без видимой карточки он читался
  как лишний зазор (было 158px сверху и 238px снизу вместо 78px, как у остальных секций).
  Паддинг обнулён, теперь отступы сверху и снизу — ровно те же 78px, что и между всеми
  другими секциями. Ряд плашек состава отделки (`.finish-features`) тоже был сделан
  full-bleed тем же приёмом. Квадрат с иконкой (`.finish-feature-icon`) перекрашен из
  полосатого паттерна в сплошную заливку фирменным оранжевым, сами иконки — в белый
  (было `color: var(--accent)`, из-за чего иконки были того же оранжевого цвета, что и
  полосатый фон).
  - **Позже по фидбэку full-bleed убран**: видео и ряд плашек выбивались влево из сетки —
    заголовок/note стояли на 27.2px правее левого края видео (было заметно как «странный
    отступ» слева от заголовка). Убрали отрицательные поля у `.finish-tour` и
    `.finish-features` — теперь оба элемента в обычном потоке `.finish-inner` и их левый/
    правый край точно совпадает с заголовком и note (проверено: все три на 63.1875px от
    края экрана на 1440px, без горизонтального overflow на 375–1920px).
  - **Заголовок и note отцентрированы**: `.finish-head` получил `justify-items: center;
    text-align: center`, `.finish-note` — `text-align: center` (было `left`) — по просьбе
    выровнять их по центру относительно блока (совпадает с центром видео ниже, проверено:
    и заголовок, и note, и видео по центру на одной и той же координате 712.5px при 1440px).
- В самом конце страницы продублирован блок «Оставьте заявку на подбор квартиры с экспертом»
  (`section.expert-lead` — второй экземпляр, идентичная разметка) сразу после «Ипотечного
  калькулятора» — чтобы форма была доступна и тем, кто долистал страницу до конца.
  По просьбе фон именно этой, последней формы (`.expert-lead__bg`, строка второго экземпляра)
  заменён на отдельное фото — семья на диване (`family-lead-bg.webp`, прислано пользователем
  через Google Диск, с уже встроенным затемнением в самом файле — дополнительный CSS-градиент
  поверх не понадобился). Первая форма (после блока «Жить — рядом») по-прежнему использует
  `fon-lead-expert.jpg` — не трогалась, просьба касалась только последней формы на странице.
- Горизонтальный внутренний паддинг у карточек-«подложек» с текстом (`.scenario-card`,
  `.trust-card`, `.offers-card`, `.walk-card`) приведён к единому значению `var(--space-27_2)`
  (27.2px) — эталон взят от `.nearby-card` (блок «Жить — рядом», 5-я секция страницы), у неё
  паддинг уже был таким. Раньше были вразнобой: 28.8px, 35.2px (все стороны), 24/36px
  (асимметрично у офферов), 32px. Вертикальные паддинги этих карточек не трогали — это
  отдельная, подобранная под конкретный контент величина.
  - Намеренно НЕ трогали: `.layouts-card` (14.4px, паддинг вокруг фото-миниатюры планировки —
    другая по смыслу величина, не текстовая карточка), `.stat-card` (паддинг на `clamp()`,
    отдельная адаптивная система), `.mcalc-row` (строка таблицы без бокового паддинга по
    задумке — растягивается на всю ширину), `.finish-feature` (иконка+текст в ряд, нет
    сопоставимого «горизонтального паддинга карточки» как понятия).
- Тот же эталон (`var(--space-27_2)`, 27.2px) распространён и на боковой паддинг
  «обёрток»-контейнеров самих секций (`.scenarios-inner`, `.offers-inner`, `.layouts-inner`,
  `.walk-inner`, `.finish-inner`, `.mcalc-inner`, `.nearby-inner`, `.expert-lead__inner`,
  `.leadgen`) — раньше там было `var(--space-94)` (94px) почти везде и `var(--space-41_6)`
  (41.6px) у `.nearby-inner`, из-за чего, например, заголовок «Выгодные условия» стоял
  заметно дальше от края белой панели, чем текст карточки «Сформированный район» от края
  своей тёмной карточки — расхождение было видно на скриншоте. `.finish-tour` (полноширинный
  3D-тур, пробивает `.finish-inner` отрицательным полем) пересчитан на новое значение паддинга,
  чтобы breakout не разъехался. Мобильные брейкпоинты (`padding: 0 var(--space-24)` на ≤640px)
  не трогали — там паддинги и так были близки друг к другу (24px везде).

- Блок «Дополнительные услуги» (`.addl`, в самом конце страницы, ПОСЛЕ финальной формы
  «Оставьте заявку...») — раньше был слайдером (одна карточка+фото видны за раз, листались
  стрелками в шапке/поверх фото и точками), затем короткое время — вертикальным списком из трёх
  пар карточка+фото. **По итоговой просьбе («три горизонтальных карточки, чтобы сразу видно,
  вообще без галерей») переверстан в три равные колонки** (`.addl-list`,
  `grid-template-columns: repeat(3, 1fr)`), которые видны на экране одновременно, без какого-либо
  JS — carousel-скрипт (`addlTrack`/`addlDots`, `[data-addl-prev/next]`) и всё, что с ним связано,
  удалены из разметки и стилей целиком ещё на предыдущем шаге. Каждая пара «карточка+фото»
  объединена в единый элемент `.addl-card` — фото-фон с затемняющим градиентом и текстом поверх,
  по образцу уже существующего на странице паттерна `.trust-photo`/`.trust-photo-overlay`/
  `.trust-photo-text` (блок «Жить — спокойно»), а не отдельная палитра «под макет»:
  - `.addl-card` — `position: relative`, `<img>` растянут на всю карточку (`position: absolute;
    inset: 0; object-fit: cover;`), поверх — `.addl-card-overlay` (тёмный градиент снизу вверх,
    `rgba(10,14,25,0)` → `rgba(10,14,25,0.85)`), поверх него — `.addl-card-body` (заголовок,
    описание, кнопка), прижатый к низу карточки. Единая цветовая логика для всех трёх карточек —
    больше нет отдельных «акцентного/нейтрального/тёмного» вариантов фона.
    - По просьбе затемнение выровнено между карточками: раньше `.addl-card-overlay` был только
      градиентом (`0%` прозрачности сверху → тёмный низ), из-за чего на светлом фото (интерьер)
      тень читалась заметно слабее, чем на тёмном (фасад) — визуально «тень» казалась разного
      размера/силы у разных карточек. Добавлена ровная полупрозрачная заливка
      (`rgba(10, 14, 25, 0.25)`) поверх фото под тем же градиентом, независимая от исходной
      яркости фото — теперь все три карточки затемнены одинаково вне зависимости от того, тёмное
      исходное фото или светлое.
  - кнопка `.addl-btn` — тот же паттерн пилюли с белым кружком-стрелкой, что у
    `.leadgen-submit`/`.icon-circle`, единый для всех трёх карточек (инверсия для «оранжевого»
    варианта больше не нужна — фон везде фото+градиент, а не заливка).
  - боковой паддинг `.addl-inner` — тот же `var(--space-27_2)`, что и у всех остальных секций.
  - На ≤1024px сетка схлопывается в одну колонку (`grid-template-columns: 1fr`), карточки
    показываются друг под другом на всю ширину.
  - **Фото первых двух карточек заменены на реальные** (были временные плейсхолдеры,
    повторно взятые из уже загруженных на сайт изображений): «Коммерческая недвижимость» —
    `commercial-storefront.webp` (фасад с вывесками «Продажа/Аренда»), «Дизайн-проект
    интерьера» — `interior-dining.webp` (гостиная с обеденной зоной, 3D-визуализация). Оба
    файла присланы пользователем через Google Диск в формате `.webp` (не конвертировались
    в `.jpg`, т.к. в системе не нашлось доступного инструмента конвертации — `.webp`
    полностью поддерживается всеми современными браузерами, на вёрстку/`object-fit` не влияет).
    `object-position: center 30%` не менялся — подходит обоим новым фото без коррекции.
    Третья карточка («Вторичная продажа») тоже заменена на реальное фото —
    `resale-night.jpg` (ночной вид ЖК), прислано пользователем через Google Диск как
    `Ночь 2(1).jpg`, JPEG 5760×3240, 13 МБ. Исходник не подходил по размеру (остальные
    фото на сайте — сотни КБ, а не десятки МБ), инструментов конвертации в системе как и
    раньше нет (`magick`/`convert`/`cwebp` недоступны), но для JPEG сработал `System.Drawing`
    из .NET через PowerShell: уменьшен до ширины 2000px (пропорционально, `2000×1125`) и
    пересохранён с качеством JPEG 78 — итоговый файл 227 КБ, в одном порядке с остальными
    изображениями сайта.

- Блок «Приезжайте — покажем на месте» (`.offices`) — новый, самый последний блок страницы.
  Первая версия была сверстана слишком буквально по референс-скриншоту (заголовок на
  несвойственных сайту начертаниях Onest 300/800, моноширинные технические подписи, отдельная
  белая CTA-кнопка капсом) — по просьбе «задизайнь именно» переделана на собственный
  визуальный язык сайта, референс остался только источником состава контента (телефон, адрес,
  список офисов), а не визуальным образцом:
  - заголовок/note — тот же приём, что у `.mcalc-head`/`.mcalc-heading`/`.mcalc-note`:
    RoadRadio 700 uppercase с акцентным `<span>`, note приглушённым `opacity: 0.45` справа —
    тот же почерк заголовка, что у всех остальных блоков страницы, а не отдельное начертание.
  - тёмная карточка контакта `.offices-contact` — тот же `#1b1c22`, что у `.leadgen`, и та же
    точечная текстура фона (`::before` с `radial-gradient`), что у `.leadgen::before` — раньше
    была голой плоской заливкой.
  - CTA `.offices-cta` — стандартная пилюля `.leadgen-submit`/`.addl-btn` (акцентная заливка +
    белый `.icon-circle` со стрелкой `→`), а не отдельная белая кнопка капсом на всю ширину,
    которую сочинили под референс. По просьбе текст кнопки отцентрован в зоне между левым краем
    кнопки и левым краем круглой иконки (был `justify-content: space-between` — текст жался к
    левому краю, а справа оставался большой пустой промежуток до кружка). Кружок выведен из
    потока (`position: absolute; right: 0.5rem`), у кнопки `justify-content: center` и
    `padding-right`, резервирующий место под иконку — тот же приём, что уже применялся у
    `.expert-lead__form .leadgen-submit`. Отдельно забыли зафиксировать высоту при первой
    вёрстке — кнопка была 34px. Сначала выровняли на 5.5rem/88px (высота
    `.expert-lead__form .leadgen-submit`), но по уточнению нужна была высота как у кнопок
    в блоках «Доп. услуги»/«Новости и статьи» (`.addl-btn`/`.news-btn`, ≈51px) — сначала
    вернули вертикальный паддинг `var(--space-8)` сверху/снизу вместо фиксированной высоты
    (тот же приём, что у этих кнопок), но это не сработало: в `.addl-btn`/`.news-btn` иконка
    `.icon-circle` стоит в потоке flex-строки и своей высотой (2.2rem) растягивает саму
    кнопку до 51px, а в `.offices-cta` иконка абсолютно спозиционирована и не в потоке — из-за
    этого паддинг-приём давал только 34px, ошибка осталась незамеченной при первой правке.
    Обнаружено и исправлено позже: `.offices-cta` получил явную `height: 3.2rem` (51.2px),
    паддинг стал только горизонтальным (`0 var(--space-48) 0 var(--space-35_2)`) — итоговая
    высота теперь точно совпадает с `.addl-btn`/`.news-btn` (51.1875px).
  - (история) карточки офисов `.offices-card` на промежуточном шаге переняли интерактивный язык
    `.offers-card` (серый фон, акцентная заливка при наведении, значок-бейдж с иконкой
    геолокации) вместо моноширинной подписи из самой первой версии — системный monospace-стек
    убран целиком ещё тогда, сайт нигде больше его не использует. Сам компонент `.offices-card`
    в дальнейшем удалён (см. ниже, «Итоговый формат») — адреса переехали внутрь тёмной карточки
    коротким списком без иконок.
  - боковой паддинг `.offices-inner` — тот же `var(--space-27_2)`, что и у всех остальных
    секций (без изменений).
  - Контактные данные (телефон `+7 (844) 220-44-13`, адрес «ул. Советская, 28», часы
    8:00–20:00) взяты из уже используемых в `.expert-lead__contacts` — не выдуманы заново.
    Остальные три офиса (ЖК «Берег Волги», ЖК «Новый», ЖК «Герои Поколений») — из референса,
    отдельно не проверялись.
  - Кнопки соцсетей (`.offices-social`, VK/ОК/Telegram) — теперь настоящие брендовые SVG-
    иконки (были текстовые инициалы «VK»/«ОК»/«TG»), кружок и hover-заливка акцентом не
    менялись. Ссылки по-прежнему заглушки (`href="#"`). **Требует замены на реальные ссылки**,
    когда появятся.
    - Иконка VK позже заменена ещё раз: официальный брендовый path VK — это иконка приложения
      с собственным встроенным скруглённым квадратом-фоном, что визуально конфликтовало с
      круглым фоном самой кнопки `.offices-social`/`.footer-social` («какой-то прямоугольный
      кривой» на фоне круга). Заменена на простую монограмму `<text>` «VK» без встроенного
      фона — чисто вписывается в существующий круглый контейнер кнопки. Применено одинаково
      в обоих местах (`.offices-social` и `.footer-social`).
    - Иконка VK заменена в третий раз на официальный круглый логотип-бейдж ВКонтакте
      (файл предоставлен пользователем, `viewBox="0 0 97.75 97.75"`, один `<path>`, внешний
      контур которого сам рисует полный круг с вырезанной внутри буквой «К» — в отличие от
      прежней версии с квадратным фоном, эта иконка уже круглая и естественно вписывается
      в круглый контейнер кнопки). `fill` на `<svg>` заменён с `#000000` на `currentColor`,
      чтобы сохранить существующую тему цвета/hover кнопки. Применено одинаково в обоих
      местах (`.offices-social` и `.footer-social`).
  - **Итоговый формат** (по референсу «Наши контакты» — узкая колонка слева + большая карта
    справа, во всю высоту колонки): `.offices-grid` — 2 колонки (`minmax(20rem, 34%) 1fr`,
    `align-items: stretch`), слева `.offices-left` с тёмной карточкой `.offices-contact`,
    справа `.offices-map` — растягивается на всю высоту левой колонки автоматически (grid
    `align-items: stretch`), а не имеет собственную фиксированную высоту.
  - **Адреса всех 4 офисов теперь внутри самой тёмной карточки** `.offices-contact`, сразу под
    телефоном — короткий список `.offices-mini-list` (название жирным + адрес обычным текстом,
    без иконок и часов работы), по просьбе «разместить кратко, без иконок». Раньше это были
    точечный пункт «Адрес главного офиса» (`.offices-address`, только один адрес) плюс отдельная
    стопка из 4 крупных карточек `.offices-list`/`.offices-card` (с иконкой-бейджем и часами)
    под контактным блоком — оба варианта убраны, соответствующие CSS-классы удалены как
    неиспользуемые.
  - **Карта офисов** (`.offices-map`) — встроена через бесплатный Yandex Maps `map-widget`
    iframe (без API-ключа), центрирована на Волгоград (`ll=44.516975,48.707067`) с одной
    меткой. **Координаты приблизительные** (центр города, не точный адрес офиса на
    ул. Советская, 28, и без меток остальных 3 офисов) — стоит заменить на точный embed-код
    из конструктора Яндекс.Карт с реальными метками всех 4 офисов, когда будут точные
    координаты.

- Блок «Новости и статьи» (`.news`) — новый, теперь самый последний блок страницы (после
  `.offices`). Состав карточек (фото сверху + заголовок + текст + кнопка «Читать статью») взят
  из референс-скриншота как источник структуры контента, но оформление — полностью на уже
  существующих паттернах сайта, референс не копировался визуально:
  - обёртка `.news-panel`/`.news-inner`/`.news-head`/`.news-heading`/`.news-note` — точная копия
    паттерна `.offices-panel`/`.offices-head` (белая панель с закруглением, RoadRadio 700
    uppercase заголовок с акцентным `<span>`, note приглушённым `opacity: 0.45` справа) — тот же
    почерк заголовка блока, что и у всех остальных секций.
    - Позже по просьбе белая подложка/панель снята (см. ниже, в подвале, карточки перекрашены
      обратно в белый уже отдельно от самой панели) — после этого собственный внутренний
      паддинг `.news-panel` (`var(--space-80) 0`) стал задваивать внешние отступы секции, тем же
      образом, что раньше произошло с `.finish-panel`. Обнулён (`.news-panel { padding: 0; }`) —
      тот же фикс, тот же приём.
    - Текст-приписка `.news-note` («Главное о ходе строительства, условиях покупки и жизни в
      комплексе.») удалена по просьбе целиком — из разметки и из CSS (включая мобильный
      override `.news-note { text-align: left; align-self: auto; }`). `.news-head` соответственно
      упрощён с грид-раскладки (`display: grid; grid-template-columns: 1fr auto;`, плюс отдельный
      мобильный override на одну колонку) до простого `margin: 0 0 var(--space-41_6);` —
      тот же heading-only паттерн, что уже используется у `.addl-head`.
  - `.news-card` — нейтральный серый фон `#f2f3f2` (как у `.offices-card`), фото сверху
    (было `aspect-ratio: 4/3`, по просьбе уменьшено по высоте на 25% —
    математически `0.75 × 0.75 = 0.5625` ширины, что ровно равно `16/9`, поэтому
    `aspect-ratio: 16/9`; `object-fit: cover`), заголовок обычным начертанием Onest 700 (не
    uppercase — в отличие от `.offices-card-kicker`, у новостных заголовков предложенческий
    регистр естественнее), описание обрезано до 3 строк (`-webkit-line-clamp`).
  - кнопка `.news-btn` — стандартная акцентная пилюля с белым `.icon-circle` (`→`), тот же
    паттерн, что у `.addl-btn`/`.offices-cta`/`.leadgen-submit`, без своего варианта кнопки.
  - **Контент — временный, для примера**: заголовки/тексты карточек написаны под сам ЖК
    «Жить хорошо» — по смыслу близко к референсу, но переписаны под этот проект, а не
    скопированы дословно. Ссылки на карточках — заглушки на `#leadgen` (открытых статей ещё
    нет). **Требует замены на реальные статьи/фото**, когда они появятся.
    - Первая карточка по просьбе заменена: заголовок «ЖК «Жить хорошо»: всё включено», текст
      «Монолит-кирпич, своя котельная, двор без машин: что из этого действительно меняет
      жизнь.» (было — «Старт продаж квартир...»). Фото на тот момент не менялось, `alt`
      синхронизирован с новым заголовком.
    - Вторая карточка тоже заменена: заголовок «Советский район Волгограда: путеводитель по
      району», текст «Что находится вокруг улицы Новосибирской — вузы, школы, медицина,
      транспорт.» (было — «Что такое эскроу-счёт и почему это безопасно»). Фото на тот момент
      не менялось, `alt` синхронизирован.
    - Третья карточка тоже заменена: заголовок «Сколько стоят четыре года учёбы студента»,
      текст «3 сценария студента — общежитие, съёмная квартира и покупка своей рядом с вузом.»
      (было — «Скидки в честь старта продаж квартир»). Фото на тот момент не менялось,
      `alt` синхронизирован. Все три карточки блока «Новости и статьи» теперь заменены на
      присланный пользователем текст (было по одному запросу на карточку).
    - Во всех трёх текстах по просьбе добавлен ручной перенос строки (`<br />`) в тексте
      описания (`p`) — в точке, где заканчивается вводная часть с двоеточием/тире, по образцу
      строк, присланных пользователем (совпадает с местом, где раньше уже применялся приём
      «так переносы сделай» — см. приписку про адрес Новосибирская, 37 выше). Ограничение
      `-webkit-line-clamp: 3` не конфликтует — текста в каждой карточке всего 2 строки.
      Позже перенос у третьей карточки («3 сценария студента…») уточнён/сдвинут на другую
      точку (после «квартира», перед «и покупка») — по новому уточнению от пользователя.
    - Отступы между заголовком (`h3`), описанием (`p`) и кнопкой внутри `.news-card-body`
      увеличены на 10% по просьбе: было `gap: var(--space-12)` (12px), стало `0.825rem`
      (13.2px). У `.addl-card-body` (блок «Дополнительные услуги») аналогичный gap изначально
      был другим (`var(--space-14_4)`, 14.4px) — по условию запроса («если у этих 2 блоков
      они одинаковые») не трогали, т.к. значения не совпадали.
  - Фото изначально были теми же временными плейсхолдерами, что и в `.addl` и в блоках
    «Семейный дом в комфортном районе» / «Все что нужно в шаговой доступности» / «Жить —
    спокойно» (`aerial-view.jpg`, `trust-photo.jpg`, `family-house.jpg` — эти три файла
    переиспользовались сразу в нескольких местах страницы), отдельных фото под темы
    новостей не было.
    - Позже по просьбе заменены на реальные фото — пользователь загрузил на Google Диск
      файлы «1 новость» (JPEG, аэрофото ЖК), «2 новость» (WebP, парк/аллея) и «3 новость»
      (WebP, портрет девушки с блокнотом), пронумерованные по порядку карточек. Файлы
      скачаны с Диска (файл «1 новость» — 14,7 МБ, превышал лимит MCP-инструмента
      `download_file_content` в 10 МБ, скачан напрямую через
      `curl "https://drive.google.com/uc?export=download&id=<fileId>"`, как и раньше для
      «Вторичная продажа»). WebP-файлы декодированы через WPF/`System.Windows.Media.Imaging`
      (у `System.Drawing`/GDI+ нет встроенного WebP-кодека — падает с «Out of memory» при
      попытке открыть `.webp`, а у WIC через WPF он есть) и пересохранены в JPEG через
      `System.Drawing`, сжаты тем же способом, что и `resale-night.jpg` (`HighQualityBicubic`,
      JPEG quality 78).
    - **Ошибка и исправление**: новые фото сначала были сохранены под старыми именами файлов
      (`aerial-view.jpg`, `trust-photo.jpg`, `family-house.jpg`) — из-за переиспользования
      этих файлов в других блоках страницы (см. выше) это непреднамеренно подменило фото и
      там тоже (например, в блоке «Жить — спокойно» вместо исходного фото парка показалось
      новое фото из новостей). Исправлено переносом: старые файлы восстановлены из git-истории
      (коммит `e2af9c1`, до подмены) обратно под исходными именами, а три новых фото с Диска
      сохранены под отдельными именами `news-aerial.jpg` (2000×1498, 605 КБ),
      `news-district.jpg` (1920×1078, 392 КБ), `news-students.jpg` (1600×889, 140 КБ) и
      подключены только в карточках `.news-card` — остальные блоки используют исходные фото
      без изменений.

- **Подвал страницы** (`.site-footer`) — раньше на странице не было вообще, добавлен с нуля
  (референс-скриншот использовался только как источник состава/раскладки — 4 колонки: бренд,
  «Разделы», «Контакты», «Документы» + нижняя строка с юридическим текстом и копирайтом — само
  оформление на паттернах сайта, не копия скриншота):
  - По просьбе фон подвала сделан тёмным (`#1b1c22`, тот же цвет, что у `.offices-contact`/
    `.leadgen`), весь текст/бордеры переведены на `rgba(255,255,255,…)` вместо тёмных. Логотип —
    больше не кружок-инициалы «ЖХ», а настоящий SVG-логотип из шапки (`.logo-svg`, тот же набор
    `<path>`, скопирован с уникальным `id` у `<clipPath>`, чтобы не дублировать `id` с шапкой) —
    он и был расчитан на тёмный фон (заливки `white`/`#F27239`), поэтому на новом тёмном подвале
    смотрится органично. Название рядом — тем же RoadRadio 700 uppercase, что и было.
    - Позже по просьбе текстовая подпись «Жить хорошо» рядом с логотипом убрана (`.footer-logo-text`
      удалён из разметки и CSS), а сам логотип увеличен на 40% (`.footer-logo-svg`: было
      `width: 2.6rem`, стало `2.6 × 1.4 = 3.64rem`). Отступ сверху у описания под логотипом
      (`.footer-desc`) чуть увеличен (`margin: var(--space-8) 0 0`, было `margin: 0`) — чтобы
      компенсировать пропавшую строку с текстом и не «слипаться» с укрупнённым логотипом.
    - Ещё позже по просьбе в сам текст описания добавлена первая строка с названием ЖК —
      `.footer-desc` теперь «Жилой комплекс «Жить хорошо» / в Советском районе Волгограда. /
      Новосибирская, 37 — строительный адрес.» (три строки через `<br />`, разбивка по
      переносам, присланным пользователем). Название дано в кавычках-«ёлочках», как и везде
      на сайте (ЖК «Жить хорошо»), а не оставлено голым текстом без кавычек.
    - Ещё позже логотип увеличен ещё на 30% (`.footer-logo-svg`: было `3.64rem`, стало
      `3.64 × 1.3 = 4.732rem`, фактическая ширина на странице ≈75.7px).
  - Карточки `.news-card` (блок «Новости и статьи») по той же просьбе перекрашены с серого
    `#f2f3f2` в белый `#ffffff` — плашки под новостями стали белыми.
  - **«Разделы»** — список продублирован из основного меню шапки (`.nav`, те же href), включая
    пункт «О нас», которого не было на скриншоте-референсе (в шапке он есть — меню приведено
    к актуальному состоянию).
  - **«Контакты»** — телефон и адрес взяты из уже используемых в `.offices-contact`
    (`+7 (844) 220-44-13`, ул. Советская 28, ежедневно 8:00–20:00) — не выдуманы заново.
    По просьбе плейсхолдер-чип `.footer-tbd` с E-mail заменён на три круглые иконки
    мессенджеров — ВКонтакте, Telegram, MAX (`.footer-social`, тот же паттерн кружков,
    что у `.offices-social`: `rgba(255,255,255,0.08)` фон, акцентная заливка на hover).
    VK и Telegram — те же брендовые SVG-иконки (пути), что уже используются в
    `.offices-social`, для единообразия (VK — монограмма `<text>`, см. выше). MAX —
    раньше обобщённый значок «чат-пузырь с тремя точками» (не верифицированный официальный
    логотип), теперь заменён на настоящий официальный бренд-SVG MAX (`viewBox="0 0 720 720"`,
    один `<path>`), присланный пользователем локальным файлом — `fill` заменён на
    `currentColor`, чтобы иконка подхватывала ту же логику цвета/hover, что и остальные кнопки
    `.footer-social`. Ссылки всех трёх иконок пока заглушки (`href="#"`) — **требуют реальных
    ссылок** на профили/каналы, когда появятся. Класс `.footer-tbd` в CSS убран как более
    неиспользуемый (это было единственное его применение в разметке).
  - **«Документы»** — по просьбе добавлены «Ход строительства» и «Проектная декларация»/
    «Разрешение на строительство» — ведут на `наш.дом.рф` (`target="_blank"`), т.к. это
    официальный государственный агрегатор такой информации по объектам, а не выдуманный адрес.
    Позже по просьбе список расширен ещё тремя пунктами: «Политика обработки персональных
    данных», «Согласие на обработку персональных данных», «Согласие на обработку данных
    метрическими программами» — реальных страниц под них на сайте пока нет, все, включая уже
    бывшую там «Политику конфиденциальности», — ссылки-заглушки (`href="#"`). **Требует
    реальных документов/страниц**, когда появятся (возможно, часть из них после появления
    юриста/реальной политики стоит объединить, если они окажутся дублирующими друг друга).
  - Нижняя строка: юридический дисклеймер полностью заменён на реальный текст застройщика
    (про ст. 435, п. 2 ст. 437 ГК РФ — не публичная оферта, актуальность информации и т.п.,
    предоставлен пользователем) — плейсхолдер-чип `.footer-tbd` для этого текста больше не
    нужен и убран вместе с CSS-правилом `.footer-legal .footer-tbd`. Копирайт тоже заменён на
    реальный: `© 2026 «А» Бутик недвижимости. Все права защищены.` — название взято из уже
    использовавшегося на странице «Центральный офис А-Бутик недвижимости» (`.offices-list`),
    это торговое наименование, **не полное юрлицо с ОГРН/ИНН** — если понадобится точная
    юридическая формулировка для копирайта, стоит уточнить у застройщика.
    - Позже по просьбе строка копирайта (`© 2026 «А» Бутик недвижимости. Все права защищены.`)
      удалена целиком — остался только юридический дисклеймер. `.footer-bottom` был flex-строкой
      `justify-content: space-between` под два элемента (текст слева, копирайт справа) —
      после удаления второго элемента убраны ставшие ненужными flex-свойства (`display: flex`,
      `align-items`, `justify-content`, `gap`) и мобильное переопределение
      `flex-direction: column`. `.footer-legal` получил `width: 100%` вместо `max-width: 64rem`
      (раньше текст упирался в фиксированный потолок ширины и «стопился» по центру контейнера
      в 5 строк, даже когда справа не было копирайта, занимающего место, — теперь растягивается
      на всю ширину `.footer-inner`). Неиспользуемое CSS-правило `.footer-copy` тоже удалено.
  - На ≤1024px сетка сжимается до 2 колонок (бренд — на всю ширину сверху), на ≤640px — в одну
    колонку.

## Акцентный цвет

- Ключевой акцентный цвет полностью заменён с `#f27239` (оранжевый) на `#F03D3A`
  (красный, по брендбуку) — обновлена переменная `--accent` в `:root`, все
  `var(--accent, #f27239)`-фолбэки и хардкод-вхождения `#f27239`/`#F27239` в SVG-иконках
  (нижний колонтитул, декоративные элементы, `.heading .accent`, значки калькулятора и т.д.) —
  всего 57 вхождений заменено на `#F03D3A`.
  - `--accent-secondary: #ff773c;` (используется только в `.max-link`) не тронут — это
    отдельный вторичный оттенок, не входит в «ключевой цвет», по запросу не менялся.

## Дизайн-токены

Все отступы (`margin`/`padding`/`gap`) с фиксированным значением в rem вынесены
в `:root` в начале `<style>` как переменные `--space-<px>` (имя = эквивалент в px
при корне 16px, например `--space-32: 2rem;`). Значения не менялись — только
заменены литералы на `var(--space-*)`, поэтому вёрстка визуально идентична.
Респонсивные интервалы на `clamp()`/`vw`/`vh` не трогали — это уже отдельная
система для плавного масштабирования, токенизировать их в фиксированную шкалу
было бы неверно.

Если понадобится единая шкала «с нуля» (напр. 4/8-точечная сетка) — стоит
завести её отдельно и постепенно мигрировать компоненты, а не переписывать
разом: часть текущих значений (2.25rem, 5.875rem, 1.625rem и т.п.) явно
подбирались вручную под конкретные блоки, а не по строгой сетке.

## Доступность (motion)

Добавлен глобальный `@media (prefers-reduced-motion: reduce)` в конце `<style>`:
сокращает все CSS-переходы/анимации до почти мгновенных для пользователей,
у которых в ОС включена настройка «уменьшить анимацию» (укачивание, вестибулярная
чувствительность и т.п.). Затрагивает hover-эффекты кнопок, открытие мобильного
меню и лид-модалки — они просто появляются без плавного скольжения. На обычных
пользователей (без этой системной настройки) изменение никак не влияет.

## 3D-иконка карточки «1-комнатные квартиры» (блок «Выгодные условия»)

Заменена плейсхолдер-иконка ключа на настоящий 3D-ассет из Figma (получен
от пользователя через Google Диск, папка «жить хорошо», файлы `1.png`/`2.png`).
Добавлен hover-свап: `assets/images/icon-key-gray.png` (серый, дефолт) и
`assets/images/icon-key-red.png` (красный, hover) — два `<img>` наложены
друг на друга абсолютным позиционированием внутри `.offers-icon`, переключение
через `opacity` на `.offers-card:hover` (см. `.offers-icon-default` /
`.offers-icon-hover` в `<style>`). Работает вместе с существующим
изменением фона карточки на hover. Карточки 3 и 4 блока по-прежнему
используют старые статичные иконки — их 3D-ассетов пока нет, заменить при
следующей синхронизации с Figma (см. комментарий в CSS `.offers`).

## 3D-иконка карточки «Семейная ипотека» (блок «Выгодные условия»)

Тот же hover-свап, что и у карточки «1-комнатные квартиры»: новый 3D-ассет
бирки с процентом получен от пользователя через Google Диск (папка
«жить хорошо», файлы `1(2).png`/`2(2).png`). Сохранены как
`assets/images/icon-percent-tag-red.png` (красный, дефолт) и
`assets/images/icon-percent-tag-gray.png` (серый, hover) — сохраняем ту же
инвертированную схему, что и для ключа (см. раздел выше): дефолтная серая
карточка показывает красную бирку, при наведении карточка становится
красной и бирка сменяется на серую. Разметка использует существующие общие
классы `.offers-icon-default`/`.offers-icon-hover`, отдельного CSS не
потребовалось. Старая иконка `icon-percent-cards.png` больше не используется
в разметке, но оставлена в `assets/images/` на случай отката.

Иконку увеличили до той же высоты, что и у ключа на карточке «1-комнатные
квартиры» (`.offers-card--2 .offers-icon { height: 24rem }`, было 17.19rem) —
по просьбе пользователя брелок должен расти по диагонали влево-вверх.
Иконка позиционируется `right:0; bottom:0` внутри карточки с
`overflow: hidden`, поэтому увеличение высоты само по себе даёт нужный
эффект — верхний и левый края разрастаются наружу и обрезаются краем
карточки, а точка привязки (правый нижний угол) не двигается.

## 3D-иконка карточки «Рассрочка до ключей» (блок «Выгодные условия»)

Тот же hover-свап на последней из четырёх карточек блока: новый 3D-ассет
стопки карт с процентом получен от пользователя через Google Диск (папка
«жить хорошо», файлы `1(3).png`/`2(3).png`). Сохранены как
`assets/images/icon-percent-stack-red.png` (красный, дефолт) и
`assets/images/icon-percent-stack-gray.png` (серый, hover) — та же
инвертированная схема, что у ключа и у бирки «Семейная ипотека». Разметка
переиспользует общие классы `.offers-icon-default`/`.offers-icon-hover`.
Старая иконка `icon-percent-circle.png` больше не используется в разметке,
но оставлена в `assets/images/` на случай отката. Иконку увеличили до
24rem — так же, как ключ и бирку на карточках 1 и 2, для единообразия
по всему блоку.

## 3D-иконка карточки «1% скидка за быстрый выход на сделку» (блок «Выгодные условия»)

Последняя, четвёртая карточка блока раньше была единственной без
hover-свапа — использовала один статичный `icon-tag-discount.png` без
инверсии цвета при наведении. По просьбе добавлен тот же 3D-ассет и та же
механика, что у остальных трёх карточек: новый 3D-ассет бирки-процента
получен от пользователя через Google Диск (папка «жить хорошо», файлы
`1(4).png`/`2(4).png`, цвет проверен сэмплингом пикселей — `1(4).png`
серо-нейтральный, `2(4).png` красно-оранжевый). Сохранены как
`assets/images/icon-tag-discount-red.png` (красный, дефолт) и
`assets/images/icon-tag-discount-gray.png` (серый, hover) — та же
инвертированная схема, что у остальных трёх иконок блока. Разметка
переиспользует общие классы `.offers-icon-default`/`.offers-icon-hover`
(было `<img src="icon-tag-discount.png">` без вариантов). Старая иконка
`icon-tag-discount.png` больше не используется в разметке, но оставлена
в `assets/images/` на случай отката. Высоту иконки (`.offers-card--4
.offers-icon`) не трогали — она и так была подобрана отдельно (18.06rem)
под этот 3D-ассет.
- Позже по просьбе уменьшена ещё на 7% (`18.06rem → 16.7958rem`). Иконка
  позиционируется `right: 0; bottom: 0` внутри карточки (тот же приём, что
  и у остальных трёх) — уменьшение высоты само по себе сдвигает её к
  правому нижнему углу, ровно то направление, которое и просили.

## Редизайн блока «Жить — спокойно» (`.trust`)

Блок выглядел плоско: серые прямоугольники с мелкой иконкой в углу поверх
текста. Обновили визуальный язык, не трогая структуру sticky-наезда:

- **Карточки `.trust-card`**: иконка переехала из угла (absolute, поверх
  текста) в отдельный блок `.trust-card-top` над заголовком — обычный поток,
  без наложений. Иконка увеличена (2.6rem → 3rem круг, svg 1.1rem → 1.4rem).
- **Hover стал заметнее**: карточка приподнимается (`translateY(-0.3rem)`) и
  получает цветную тень в тон акцента, вместо просто смены фона.
- **Стрелка-аффорданс** (`.trust-card-arrow`) в углу, появляется только на
  hover — тот же визуальный язык, что у `.offers-btn` в блоке «Выгодные
  условия» (белый кружок, красная стрелка), для связности между блоками.
- **Фото-панель**: добавлен лёгкий kenburns-эффект
  на фото (`scale(1.06)` → `scale(1)` при hover), на мобильном отключён
  (нет hover) через `transform:none`.
- Заголовок `h2` увеличен до 3.375rem на десктопе, слово «спокойно» выделено
  акцентным цветом через `<span>` — раньше весь заголовок был одним цветом.
- Попутно поправили баг: в двух карточках текст переноса (`<br
  class="br-desktop">`) слипался в одно слово на мобильном, потому что перед
  `<br>` не было пробела, а сам `<br>` на мобильном скрыт (`display:none`) —
  добавили пробел.
- **Вертикальные отступы у заголовка/подзаголовка**: из-за разной длины
  текста в карточках снизу оставалось «лишнее» пустое место, не равное
  отступу сверху (у иконки). Первая попытка (`justify-content: center` на
  `.trust-card`) была неверной — она сдвигала вниз и иконку тоже. Правильное
  решение: `margin: auto 0 var(--space-9_6)` на `.trust-card h3` — иконка
  (`.trust-card-top`) остаётся на исходном месте сверху, а заголовок с
  подзаголовком «прижимаются» вниз auto-margin'ом, забирая свободное
  пространство. В итоге отступ сверху (до иконки) и снизу (после текста)
  теперь совпадают.
- **Убрали цифры-водяные знаки** (`.trust-card-num`, 01/02/03) — по
  фидбэку это был лишний визуальный шум. Удалили HTML-спаны и связанные
  CSS-правила (базовое, hover, мобильный `@media`).
- **Убрали стеклянный бейдж «Закрытая территория»** (`.trust-photo-badge`)
  поверх фото — тоже посчитали лишним шумом. Удалили HTML-блок и связанные
  CSS-правила (базовое, мобильный `@media`).
- **Убрали бордер у карточек** (`.trust-card`: `border: 1px solid rgba(28, 28,
  26, 0.05)`, плюс `border-color` в `transition` и в `:hover` — везде убрано)
  по прямой просьбе «бордер удали у плашек».
- **Стеклянные плавающие карточки поверх фото (локальный эксперимент)**: по
  референсу переделали расклад блока — фото теперь на всю ширину
  `.trust-inner` (`.trust-photo { position: absolute; inset: 0; }`), а три
  карточки `.trust-cards` наложены поверх него справа
  (`position: absolute; top/right/bottom: var(--space-32)`,
  `width: min(27rem, 38%)`), вместо прежней grid-раскладки «фото слева,
  карточки в столбик справа». Карточки получили стекло-эффект:
  `background: rgba(17, 19, 26, 0.45)` +
  `backdrop-filter: blur(20px) saturate(160%)` (с `-webkit-` префиксом),
  текст перекрашен в белый (`h3` — `#ffffff`, `p` —
  `rgba(255, 255, 255, 0.7)`), иконка `.trust-icon` стала скруглённым
  квадратом (`border-radius: 0.9rem` вместо круга) как на референсе.
  На мобильном (`@media max-width: 900px`) раскладка откатывается к
  прежнему виду: `.trust-inner` — блочная, фото — обычный блок
  (`position: relative`, без kenburns-transform), карточки — статичные,
  в столбик, без стекла (`background: #eef1f5`, `backdrop-filter: none`,
  текст снова тёмный). Проверено в превью на 1440px (фото на всю ширину
  816px, карточки 432×240 справа, `backdrop-filter` рендерится, hover
  по-прежнему красит карточку в акцентный красный) и на 375px (нет
  горизонтального overflow, карточки опрощены до непрозрачных светлых
  плашек). **Важно**: это временное изменение только для локальной проверки
  дизайна — по просьбе пользователя пока не запушено на GitHub Pages,
  осталось только в рабочей копии/локальном коммите.
- **Заменено фото фона** на `assets/images/trust-photo.jpg` (новый аэрофотоснимок
  стройки, загружен на Google Диск и скачан через Drive MCP) — старое фото
  заменено тем же именем файла, чтобы не трогать разметку. Оригинал с Диска
  весил 2.4 МБ (2459×1184), пережат через `ffmpeg -q:v 4` до 339 КБ без
  изменения разрешения — сопоставимо по весу с другими фото секции
  (`aerial-view.jpg` ~937 КБ, `family-house.jpg` ~589 КБ). Проверено в
  превью на 1440px: изображение полностью загружается и заполняет
  `.trust-photo` (2459×1184 natural → 1434×865 отображаемых).
- **Починили высоту sticky-наезда**: блоки 3/4/5 (`.scenarios`/`.trust`/
  `.nearby`) — это `position: sticky` стопка, где каждый следующий блок
  должен быть НЕ НИЖЕ предыдущего, иначе при наезде остаётся видна
  полоска блока снизу (ровно то, на что пожаловались: «видно 3 блок под
  4 и 5»). После редизайна `.trust-inner` получил `min-height: 51rem`
  (816px) без проверки против фактической высоты `.scenarios` (864px на
  1440px) — блок 4 стал на 48px короче блока 3 и переставал его
  полностью закрывать. Подняли `min-height` до `56rem` (896px) — теперь
  блок 4 (896px) ≥ блок 3 (864px) и блок 5 (896px) ≥ блок 4 (896px) на
  всех проверенных десктопных ширинах (1280/1440/1920px). **Известная
  отдельная проблема (не трогали)**: на ширинах ≤900px `.scenarios`
  становится очень высоким (карточки в столбик, до ~1900px), а
  `.trust`/`.nearby` на мобильном сбрасывают `min-height` на `auto` —
  разница огромная (почти 1000px), sticky-наезд на мобильном явно не
  подогнан. Это не связано с сегодняшним редизайном фото/карточек,
  похоже на существовавшую раньше проблему — требует отдельного разбора.

## Плашки-статы в блоке «Три разных сценария» (`.scenario-stats`)

По референсу (скриншот с уютной гостиной, статы в виде круглых чипов)
переделали нижний ряд характеристик карточки — было: одна строка
«Формат: … Площадь: … Цена: …» под горизонтальным разделителем
(`border-top: 1px solid rgba(255,255,255,0.22)`); стало: каждая пара
«лейбл: значение» — отдельная плашка-пилюля (`border-radius: 999px`,
полупрозрачный светлый фон `rgba(255,255,255,0.12)`, паддинг
0.45/0.9rem), без разделительной линии, `gap: var(--space-8)` между
чипами. Разметку карточек и сам фон блока (`scenarios-bg.jpg`) не
трогали — по просьбе меняли только стиль статов. Проверено в превью:
на 1440px три плашки в ряд без переносов, на 375px чипы переносятся
по `flex-wrap`, оverflow-x нет ни на одной из проверенных ширин.
**Не запушено** — как и остальная работа над блоками 3/4/5 в этой сессии,
только локальный коммит для проверки.
- **Донастройка по фидбэку**: чипы стояли слишком далеко от текста, а
  иконка планировки была заметно короче всего блока текст+чипы (пустое
  место под ней). Причина отступа — двойной зазор: `.scenario-card` уже
  имел `gap: var(--space-24)` между flex-элементами, а `.scenario-stats`
  как отдельный flex-item с `flex-basis: 100%` добавлял ещё и свой
  `margin-top: 14.4px` — суммарно 38.4px вместо задуманных ~14px.
  Исправили структурно: `.scenario-stats` перенесли внутрь
  `.scenario-body` (после `<p>`, в HTML всех трёх карточек) — теперь это
  обычный блочный элемент в потоке текста, а не отдельный flex-item
  карточки, лишний `gap` родителя на него не действует. Заодно
  `.scenario-card` получил `align-items: stretch` (было `flex-start`) —
  иконка растягивается по высоте вровень с текстом+чипами сверху и
  снизу, как на референсе (раньше высота иконки была фиксирована
  `5.2rem` и не зависела от текста). На мобильном (`@media max-width:
  900px`, `flex-direction: column`) явно вернули `align-items:
  flex-start` — иначе `stretch` растянул бы иконку на всю ширину
  карточки (кросс-ось меняется местами при column). Проверено: отступ
  текст→чипы теперь 14px (было 48px), иконка и текстовый блок совпадают
  по top/bottom координатам, нет overflow на 375/1440px.
- **Иконка приведена к квадрату**: после `align-items: stretch` высота
  иконки стала зависеть от текста (растянутый прямоугольник), а ширина
  оставалась фиксированной `5.4rem` — визуально вытянутый прямоугольник
  вместо квадрата. Убрали `width: 5.4rem`, добавили
  `aspect-ratio: 1 / 1` — ширина подтягивается к той же величине, что и
  растянутая по stretch высота, иконка остаётся квадратом на любой
  высоте текстового блока (130×130px на 1440/1920px). На мобильном
  (`@media max-width: 900px`) `align-items` уже `flex-start` (кросс-ось
  горизонтальная, не растягивается), поэтому там для `aspect-ratio`
  нужен явный `width: 5.4rem` — без него не от чего вычислить высоту,
  иконка бы схлопнулась. Проверено: 130×130px на 1440/1920px, 86×86px
  на 375px, нет overflow.
- **Перенос строки в карточке «Родителям студента»**: по просьбе слово
  «рядом» перенесли на вторую строку — было «…Медицинский колледж —
  рядом.<br />Четыре года…», стало «…Медицинский колледж<br /> — рядом.
  Четыре года…» (`<br />` в HTML сдвинут перед тире). Текст по-прежнему
  укладывается в 2 строки на 1440px и на 375px, overflow нет.
- **Однородные отступы карточки**: `padding` у `.scenario-card` был
  задан тремя разными значениями по сторонам (`var(--space-24)
  var(--space-27_2) var(--space-14_4)` — 24px сверху, 27.2px слева/справа,
  но всего 14.4px снизу), из-за чего нижний отступ был заметно теснее
  остальных трёх сторон — именно это и было видно на скриншоте с
  подписанными стрелками. Заменили на единый `padding: var(--space-24)`
  (24px со всех четырёх сторон). Проверено через `getComputedStyle`:
  `paddingTop/Right/Bottom/Left` все равны `24px`; overflow-x нет на
  1440px (`scrollWidth: 1265` при `innerWidth: 1280`).
- **Плашки-статы прижаты к нижнему краю иконки**: квадратная иконка
  (`aspect-ratio: 1/1`, растянутая по `align-items: stretch` до высоты
  `.scenario-body`) на десктопе часто оказывается выше, чем реально нужно
  тексту — раньше эта разница оставалась пустым местом ПОД плашками, и
  чипы не доходили до нижнего края картинки, что и было видно на
  скриншоте со стрелкой. `.scenario-body` сделали `display: flex;
  flex-direction: column`, а `.scenario-stats` получил `margin-top: auto`
  вместо фиксированного `var(--space-14_4)` — теперь весь «лишний» остаток
  высоты уходит в зазор ПЕРЕД плашками, и они всегда прижаты к низу вровень
  с иконкой (проверено: `iconBottom − statsBottom === 0` на всех 3
  карточках на 1440px). Минимальный отступ от текста в 14.4px
  гарантирован `margin-bottom` самого `<p>` (было `margin: 0`).
  Побочный эффект пришлось чинить отдельно: на мобильном (`@media
  max-width: 900px`) карточка становится `flex-direction: column`, и в
  этом контексте `flex: 1 1 20rem` у `.scenario-body` трактует `20rem`
  как БАЗОВУЮ ВЫСОТУ (а не ширину, как в row-раскладке) — тело
  раздувалось до 320px, а `margin-top: auto` утягивал плашки вниз с
  огромным пустым зазором сверху текста. В мобильном медиа-запросе
  добавили `.scenario-body { flex-basis: auto; }` (высота по контенту) и
  вернули плашкам фиксированный `margin-top: var(--space-14_4)` — на
  мобильном иконка идёт НАД текстом, а не сбоку, так что «прижать к низу
  иконки» там не имеет смысла. Проверено на 375px: overflow нет, отступ
  текст→чипы снова 14.4px.

## Подзаголовки секций — под заголовком, не сбоку

В нескольких секциях (`.layouts-head`, `.finish-head`, `.mcalc-head`,
`.offices-head`) подзаголовок-note раньше стоял справа от заголовка через
`grid-template-columns: 1fr auto` (`align-self: end; text-align: right`),
и только на узких экранах (медиа-запросы `@media max-width: 1200/1024px`)
переезжал под заголовок. По просьбе сделали расположение «note под
заголовком» дефолтным на всех ширинах: `grid-template-columns: 1fr`,
`gap: var(--space-16)`, `text-align: left`, без `align-self: end`. Старые
медиа-запросы, которые дублировали ту же стековую раскладку на мобильных
шириных, удалены как избыточные — теперь это и есть поведение по умолчанию.
`.walk-head`, `.addl-head`, `.news-head`, заголовки офферов и сценариев note
рядом не имели — их не трогали.

## Однострочные заголовки/note у секций

По просьбе несколько пар «заголовок + note» сведены в одну строку каждая
(было по 2 строки из-за `<br>` или узкого `max-width`): `.layouts-heading`/
`.layouts-note`, `.finish-heading`/`.finish-note`, `.mcalc-heading`/
`.mcalc-note`, `.offices-heading`/`.offices-note`. Убрали `<br>` из разметки,
`max-width` заменили на `none` + `white-space: nowrap`. Так как текст
довольно длинный (особенно у «Планировок»), на средних ширинах он бы
вылезал за пределы контейнера — поэтому для каждой пары есть свой
breakpoint отката на `white-space: normal` (`.layouts-*` — 1300px,
`.finish-*`/`.mcalc-*` — 1200px, `.offices-*` — 1024px), а ниже 640px общий
откат на обычный перенос для всех разом (там же, где уменьшается
font-size заголовков). Пороги подобраны по фактической ширине текста —
проверено на 375–1920px, нигде нет горизонтального overflow.

- **`.layouts-heading` позже возвращён обратно на 2 строки** по отдельной
  просьбе («тут заголовок в 2 строки верни») — вернули `<br />` в разметке
  между «однушки» и «до просторной трёшки», `max-width: 40rem` и убрали
  `white-space: nowrap`. `.layouts-note` при этом остался однострочным, но
  текст сокращён до одного предложения: «162 квартиры · площади от 30,81 до
  68,71 м².» (была ещё вторая часть текста — убрана). `.layouts-heading`
  убран из breakpoint'а `@media max-width: 1300px` (там ему больше нечего
  откатывать — он снова обычный `normal` по умолчанию), `.layouts-note`
  в нём остался.

## Отступы у блоков без «подложки»

По просьбе «у блоков без подложек должен быть отступ как у блоков с
плашками» найден и исправлен последний оставшийся случай задвоенного
отступа: `.walk-panel` (блок «Всё нужное — в пешей доступности») у него,
как и явно указано в соседнем комментарии в коде, нет белой подложки
(«блок лежит прямо на фоне страницы»), но при этом у него всё ещё был
внутренний `padding: var(--space-80) 0` — тот же паттерн, что раньше уже
чинили у `.finish-panel` и `.news-panel` после снятия с них белого фона
(см. комментарии в коде и разделы выше): без видимой карточки этот паддинг
не «внутри плашки», а просто задваивает внешний `margin-bottom` секции,
из-за чего вертикальный отступ у безподложечного блока получается заметно
больше, чем у карточных соседей. Обнулён (`.walk-panel { padding: 0; }`) —
тот же фикс, тот же приём, что и у `.finish-panel`/`.news-panel`. Проверены
все остальные `*-panel` блоки на странице — у всех с белым фоном паддинг
`var(--space-80) 0` намеренно оставлен (это и есть «подложка», отступ
внутри нужен), у блоков без фона (`.finish`, `.news`, теперь и `.walk`)
паддинг обнулён везде одинаково.

## Заголовок и подзаголовок модалки «Оставьте заявку»

По скриншоту с указанной стрелкой (`#leadModalTitle`, всплывающая форма
справа) заголовок «Оставьте заявку / и мы вам перезвоним» уже содержал
нужный `<br />` между строками, но реально рвался на 4 строки — у
`.lead-modal__title` был `max-width: 15rem` (240px), а содержимое модалки
шире (352px = 26.5rem модалки минус 2×36px паддинга), так что каждая
из двух заданных строк сама переносилась ещё раз. Увеличили `max-width`
до `22rem` — теперь ровно 2 строки, как просили.

Заодно расставили ручные переносы в `.lead-modal__text` (подзаголовок) по
присланному образцу: «Подберём подходящие квартиры / под ваш бюджет и
задачу, расскажем / об условиях покупки и ипотеке.» — было без `<br />`,
рвалось по ширине контейнера как придётся. Проверено через
`getBoundingClientRect()/lineHeight`: заголовок — 2 строки, подзаголовок —
3 строки, на 1440px и 375px, overflow нет.
- **Перенос в чекбоксе согласия**: по образцу «Согласен с политикой
  обработки / персональных данных» добавили `<br />` перед «персональных»
  в `.lead-modal__consent span` (раньше переносилось по ширине как
  придётся — «…персональных» / «данных»). Проверено: 2 строки на 1440px
  и 375px, overflow нет.
**Не запушено** — как и остальная работа этой сессии, только локальный
коммит для проверки.

## Фото менеджеров в модалке заявки

По просьбе «фотки обнови, на диске загрузил» заменили `assets/images/manager-1.jpg`
и `assets/images/manager-2.jpg` (аватарки в блоке «Менеджеры отдела продаж»
модалки `#leadModalBody`) на два новых фото с Google Drive (`6556А.png`,
`Ц466Ц4.png`). Исходники — PNG 621×621 с альфа-каналом; сплющили альфу на
белый фон и пересжали в JPEG через ffmpeg (`format=rgba,pad=...@1,format=rgb24`,
`-q:v 3`) — на выходе 621×621, 46–61KB, тот же порядок величины, что у
остальных фото на сайте. Пути в HTML не менялись (`.lead-modal__avatar-photo`
сама вписывает фото в круг через `border-radius: 50%; object-fit: cover`).
Проверено через `naturalWidth/naturalHeight` в превью — оба изображения
загружаются, 621×621.

## Шрифты

- **RoadRadio** — заголовки, self-hosted (`assets/fonts/RoadRadio-Bold.ttf`), только вес 700.
- **Onest** — весь остальной текст, подключается из Google Fonts (`<link>` в `<head>`), требует интернет при разработке.
